import * as cp from 'child_process';

export default class celfmt{
    
    private static kill(){
//        if (celcmtElf != null) celcmtElf.kill()
    }
    public static restart(){
        celfmt.kill();
        
  //      merlin = cp.spawn(merlinPath, []);
 //       merlin.on('exit', function(code) {       
    }
}